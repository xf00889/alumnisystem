{% extends 'base_landingpage.html' %}
{% load static %}

{% block title %}Welcome to NORSU Alumni{% endblock title %}

{% block page_header %}
{% if user.is_authenticated and not user.is_superuser %}
<div class="container">
    <div class="row align-items-center">
        <div class="col-12">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item">
                        <a href="{% url 'core:home' %}">
                            <i class="fas fa-home"></i>
                        </a>
                    </li>
                    <li class="breadcrumb-item active">Home</li>
                </ol>
            </nav>
            <p class="page-title">Welcome, {{ user.get_full_name }}</p>
        </div>
    </div>
</div>
{% endif %}
{% endblock %}

{% block extra_css %}
<style>
    /* Modern Professional Design - NORSU Alumni System */
    :root {
        --primary-color: #2b3c6b;
        --secondary-color: #4a5568;
        --accent-gold: #c8a882;
        --success-green: #28a745;
        
        --ui-background: #f7fafc;
        --ui-surface: #ffffff;
        --ui-border: #e2e8f0;
        --ui-hover: #edf2f7;
        
        --text-primary: #2d3748;
        --text-secondary: #718096;
        --text-muted: #a0aec0;
        --text-light: #ffffff;
        
        --shadow-sm: 0 2px 4px rgba(0, 0, 0, 0.06);
        --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.08);
        --shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.12);
        --shadow-xl: 0 12px 40px rgba(0, 0, 0, 0.15);
        
        --radius-sm: 0.25rem;
